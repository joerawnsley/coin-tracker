# set app entrypoint
[tool.fastapi]
entrypoint = "src.app:app"

# declare root directory for tests
[tool.pytest.ini_options]
pythonpath = ["."]
filterwarnings = [
    "ignore:'asyncio.iscoroutinefunction' is deprecated:DeprecationWarning:slowapi.extension",
]